# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

LUM (Local Universal Machine) — Warp 스타일 블록 기반 AI 터미널 에뮬레이터. xLLM(TabbyAPI/ExLlamaV2)을 기본 AI 백엔드로 사용하며, Gemini Cloud API 폴백을 지원한다. 비용 제로, 개인정보 보호를 목표로 한다. Ollama는 불필요하다.

## Build & Dev Commands

```bash
npm install                  # 프론트엔드 의존성 설치
npm run tauri dev            # 개발 모드 실행 (Rust + Vite HMR)
npm run tauri build          # 프로덕션 빌드 (경량 — burn/wgpu 제외)
npm run tauri build -- --features local-ai  # 로컬 AI 추론 포함 빌드
./run.sh                     # (선택) Ollama 자동 시작 + 개발 모드 실행 (macOS/Linux)
run.bat                      # (선택) Ollama 자동 시작 + 개발 모드 실행 (Windows)
```

> **참고**: xLLM(TabbyAPI)만 사용한다면 `npm run tauri dev` 직접 실행으로 충분. run.sh/run.bat은 Ollama 선택적 사용 시 편의 스크립트.

## Testing Commands

```bash
npm test                     # Vitest 실행 (프론트엔드 단위/통합 테스트)
npx playwright test          # E2E 스모크 테스트 (Tauri invoke 모킹, Vite 서버 필요)
npx playwright test --ui     # E2E UI 모드
cd src-tauri && cargo test   # Rust 단위 테스트 실행
```

## Architecture

**Tauri v2 앱** — Rust 백엔드 + React/TypeScript 프론트엔드. 커스텀 타이틀바 (decorations: false).

### Backend (`src-tauri/src/lib.rs`)
- **PTY 관리**: `HashMap`을 통해 탭/팬별 독립적인 PTY 세션 관리.
- **심층 RAG**: `index_project` 및 `search_codebase` 커맨드를 통해 소스코드 청킹 및 임베딩 벡터 검색 지원.
- **AI 연동**: xLLM(TabbyAPI/ExLlamaV2) 기본 + Gemini Cloud API 폴백. 임베딩 기반 의미론적 검색 및 지능형 프로젝트 요약. xLLM 서버 주소는 설정에서 변경 가능 (기본값: `http://127.0.0.1:5000`).
- **모델 관리**: 모델 다운로드(pull) 및 삭제(delete). 스트리밍 방식으로 진행률 공유.
- **주요 커맨드**: `spawn_pty`, `write_to_pty`, `generate_ai_command`, `generate_embedding`, `pull_model`, `delete_model`, `create_file`, `load_config`, `save_config`, `index_project`, `search_codebase`.

### Frontend (`src/`)
- **App.tsx**: 메인 레이아웃 (~370줄). 커스텀 훅으로 상태를 위임하고 렌더링만 담당.
- **커스텀 훅** (`src/hooks/`):
  - `useTabManager` — 탭/팬 상태 + 세션 저장/복원 (`~/.lum_session.json`, 1초 디바운스). `SshProfile` 타입 포함.
  - `useAutoHealing` — 터미널 출력 에러 감지 → AI 분석 → 안전도 배지 → PTY 실행
  - `usePanelVisibility` — 모달·사이드패널 show/hide 상태 일괄 관리
  - `useUpdateCheck` — GitHub Releases API 버전 비교 + `tauri-plugin-updater` 기반 다운로드/설치/재시작
  - `useCommandBlocks` — OSC 133 파싱, 커맨드 블록 히스토리
  - `useSshProfiles` — SSH 프로필 저장/불러오기 (`~/.lum_ssh_profiles.json`)
  - `useTerminalBlocks`, `useAIProcessing`, `useHardwareSpecs` — AI·하드웨어 레이어
- **ErrorBoundary** (`src/components/ErrorBoundary.tsx`): 터미널·패널 크래시 격리. `label` prop으로 위치 식별.
- **E2E 테스트** (`e2e/`): Playwright 스모크 테스트 5개. Tauri invoke 모킹(`e2e/setup/tauri-mock.ts`).
- **UI/UX**: `react-resizable-panels`(스플릿), `react-virtuoso`(가상 스크롤), `react-markdown`(AI 답변).
- **영속성**: `.lum_session.json`, `.lum_config.json`, `.lum_code_index.json`, `.lum_ssh_profiles.json`을 통한 데이터/설정 유지.

## Tech Stack

- **Rust**: Tauri v2, portable-pty, tauri-plugin-updater, tauri-plugin-process, ignore, reqwest, futures-util
- **Frontend**: React 19, TypeScript, Tailwind CSS v4, Vitest, Playwright, Fuse.js, react-markdown, react-resizable-panels, react-virtuoso, PrismJS
- **AI**: xLLM(TabbyAPI/ExLlamaV2) 로컬 서버, Gemini Cloud API 폴백
- **Cargo Features**: `local-ai` — burn/wgpu/tokenizers/hf-hub 포함 (기본 빌드에서 제외, 배포 바이너리 ~150MB 절감)

## Key Conventions

- 한국어 응답, 한국어 주석 필수.
- TDD 준수: 핵심 로직(`src/utils.ts`, `lib.rs`) 변경 시 테스트 코드 확인.
- AI 워크플로우: AI가 제안한 액션을 UI에서 승인 후 단계별 실행.

## 🚀 2026 Future Roadmap
- [x] Phase 10: Agent Swarms (Planner, Coder, Reviewer, Tester 협력 완료)
- [x] Gemini Integration: Gemini 1.5 Flash/Pro 지원 및 GEMINI_SYSTEM_MD 연동 완료
- [x] Phase 11: WebGPU Local AI (Burn-LM/wgpu 인프라, 토크나이저 및 추론 파이프라인 완료)
- [x] Phase 12: Secure Sandbox (정적 명령어 분석 및 차단 시스템 완료)
- [x] Phase 13: MCP Integration (stdio 기반 외부 도구 연동 완료)
- [x] Phase 14: Multi-Modal Visual Shell (데이터 시각화 및 차트 렌더링 완료)
- [x] Phase 15: Semantic Memory (의미 기반 장기 기억 엔진 완료)
- [x] Phase 16: Voice-to-Terminal (음성 인식 인터페이스 인프라 완료)
- [x] Phase 17: Autonomous Self-Healing (에러 분석 및 자율 치유 루프 완료)
- [x] Phase 18: Multi-Agent Distributed Swarms (P2P 분산 군집 인프라 완료)
- [x] Phase 19: AI Native UI Designer (실시간 React/JSX UI 생성 완료)
- [x] Phase 20: Neural Desktop Integration (OS 시각 인지 및 제어 완료)
- [x] Phase 21: Neural Vision & OS Autonomy (자율 피드백 루프, 멀티액션, 시각적 목표 검증 완료)
- [x] Phase 22: xLLM Migration & Model Manager (Ollama → TabbyAPI/ExLlamaV2, 하드웨어 인식 추천, HuggingFace 다운로드/삭제 UI 완료)
- [x] Phase 23: Real PTY Terminal (portable-pty 채널 아키텍처 + xterm.js 렌더링, 실제 셸 실행 검증 완료)
- [x] Phase 24: Cross-Platform Polish (platform.rs 모듈, Wayland 감지 가드, 번들 타겟 명시화, audio stub 정직화 완료)
- [x] Phase 25: AI Self-Healing Loop (터미널 출력 에러 감지 → analyze_error AI 분석 → 수정 커맨드 안전도 배지 + PTY 실행 완료)
- [x] Phase 26: Shared RAG Swarm (index_project/search_codebase/generate_embedding + gossipsub RagQuery/RagResult 피어 간 벡터 검색 공유 완료)
- [x] Phase 27: Multi-Tab PTY (탭 상태 관리, close_pty 커맨드, Cmd+T/W 단축키, CSS show/hide 마운팅, ptyWriteRefs Map 패턴 완료)
- [x] Phase 28: Split Pane Terminal (react-resizable-panels Group/Separator, 수평·수직 분할, activePaneId 포커스 추적, Cmd+Shift+D/E 단축키 완료)
- [x] Phase 29: Command Blocks (OSC 133 Shell Integration, zsh/bash preexec/precmd 훅 주입, useCommandBlocks 파서, CommandBlockBar UI, 리스트 뷰 히스토리 완료)
- [x] Phase 30: Semantic History Search (history.rs 임베딩 저장, search_history/get_recent_history 커맨드, HistorySearch 모달, Ctrl+R 캡처 인터셉트 완료)
- [x] Phase 31: AI Commit Message (git.rs generate_commit_message, git diff --cached + xLLM, CommitPanel UI, Cmd+Shift+G 단축키 완료)
- [x] Phase 32: xLLM 실전 최적화 (① PD Disaggregation 자동 감지, ② Elastic Scheduling model_for_task, ③ KV Cache Q4/Q8/FP16, switch_xllm_model, XllmPanel UI 완료)
- [x] Phase 33: CLI Ghost Text Autocomplete + Session Persistence (cliSpecs.ts 10개 툴 DB, ghostText.ts findCompletion, TerminalPane Tab 인터셉트 + 오버레이, session.rs save/load_session, App.tsx 1초 디바운스 자동 저장 + 마운트 복원 완료)
- [x] Phase 34: AI Inline Edit (# 프리픽스 → generate_ai_command 600ms 디바운스 → ⚡ AI 팝업 + Tab 확정, modelRef 패턴으로 useEffect 재실행 없이 최신 모델 반영 완료)
- [x] Phase 35: AI Context Awareness (context.rs get_project_context — Node/Rust/Go/Python/Java 자동 감지 + git 여부, get_recent_history 5개 병렬 조회 → context 문자열 구성, generate_ai_command에 주입 완료)
- [x] Phase 36: Auto Update Check (updater.rs check_for_update — GitHub API /releases/latest 조회, semver 비교, App.tsx 업데이트 배너 + 다운로드 링크 완료)
- [x] Refactor: App.tsx 훅 분리 (776줄 → 370줄, useTabManager/useAutoHealing/usePanelVisibility/useUpdateCheck 추출, ai.rs 미사용 model_for_task 제거 완료)
- [x] Phase 37: SSD + Dynamic Sparse Attention + EPD Streaming (xllm_body 헬퍼, draft_model·speculative_ngram, attention_sink_size·top_k_attn, call_xllm_stream SSE 파싱 → xllm_token 이벤트 → streamAICommand → 캔버스 실시간 렌더링 완료, XLLM_TOKEN_EVENT 상수화·drain 최적화·64KB 버퍼 가드 완료)
- [x] Phase 38: First-Run Onboarding Wizard (5단계 모달, 하드웨어 자동 분석, xLLM 서버 확인, 모델 다운로드, onboarding_completed 플래그 완료)
- [x] Phase 39: AI Diff Reviewer (analyze_diff — git diff/--cached + RISK:|PATH:|NOTE: 파싱, 파일별 safe/caution/risk 배지, DiffReviewPanel 아코디언 UI, Cmd+Shift+R 단축키 완료)
- [x] Phase 40: Terminal Themes & Font (5개 빌트인 테마 Dracula/Tokyo Night/One Dark/Solarized/GitHub Dark, 폰트 크기 슬라이더, 폰트 패밀리 선택, useTerminalTheme 훅, TerminalPane 동적 적용, save_terminal_appearance 커맨드, Cmd+, 단축키 완료)
- [x] Phase 41: Quick Actions (즐겨찾기 커맨드 바 — useQuickActions 훅, QuickActionsBar/QuickActionsEditor UI, Cmd+Shift+Q 토글, Cmd+1~9 단축키, save_quick_actions 커맨드, 순서 변경/삭제/편집 완료)
- [x] Phase 42: Smart Tab Rename & Auto-Icon (더블클릭 인라인 rename, OSC 7 cwd 감지 → inferTabIcon 아이콘 자동 변경, Tab.cwd/icon 필드, updateTabCwd 액션 완료)
- [x] Phase 43: Terminal Search (Cmd+F — @xterm/addon-search, 검색 바 오버레이, 대소문자·정규식 토글, Shift+Enter 이전/Enter 다음 네비게이션 완료)
- [x] Phase 44: AI Explain Command (? 프리픽스 → explain_command 500ms 디바운스 → 초록 팝업 설명, xLLM/Gemini 폴백, # = 자연어→커맨드 / ? = 커맨드→설명 대칭 UX 완료)
- [x] Phase 45: Long-Running Command Notifier (useCommandNotifier — 10초 이상 커맨드 완료 시 브라우저 Notification API + 탭 타이틀 플래시 ✅/❌ 완료)
- [x] Phase 46: Workspace Save & Restore (workspace.rs save/list/delete_workspace, WorkspaceTab cwd 저장, WorkspacePanel UI, restoreTabs 액션, TerminalPane cwd prop 전달, Cmd+Shift+S/O 단축키 완료)
- [x] Bug Fix Round 1: TerminalPane useEffect deps 버그 (cwd/onOutput/onReady 를 ref로 분리 → cd 명령 시 PTY 재마운트 방지), SessionTab color/group 필드 누락, explain_command 연산자 우선순위, fork bomb 정규식 `\(\)` 수정, useQuickActions 모듈 스코프 타이머 → useRef 이전, RagPanel 리스너 race condition, XllmPanel setTimeout 누수, rag.rs reqwest 타임아웃 추가 완료
- [x] Bug Fix Round 2: git.rs 한국어/멀티바이트 UTF-8 경계 패닉 (`chars().take(N)` 수정), ai.rs explain_command reqwest 타임아웃 추가, rag.rs/history.rs 하드코딩 XLLM_BASE → config.xllm_url() 동적 조회, CommandPalette/HistorySearch 빈 리스트 ArrowDown 음수 인덱스, WorkspacePanel deleteConfirm 상태 미초기화 완료
- [x] Windows Compat: run.bat Ollama 경로 하드코딩 → %LOCALAPPDATA%/%ProgramFiles%/PATH 자동 탐색, tabIcon.ts Windows 경로 구분자 `\` 정규화, terminal.rs PowerShell(pwsh/powershell) OSC 133 shell integration 추가 완료
- [x] Phase 47: SSH Terminal (spawn_ssh_pty — 시스템 ssh 바이너리를 portable-pty로 실행, write/resize/close 기존 PTY 인프라 재사용, SshConnectModal UI, Cmd+Shift+H 단축키, 탭 바 🔒 SSH 인디케이터 완료)
- [x] Phase 48: CLI Autocomplete DB 확장 (cliSpecs.ts 10개 → 38개: yarn/pnpm/python/pip/docker-compose/terraform/helm/rsync/make/ps/kill/tar/chmod/apt/brew/ping/cat/tail/head/wc/sed/awk/vim/systemctl/journalctl/lsof/netstat/scp 추가 완료)
- [x] Phase 49: Smart Diff Truncation (git.rs smart_truncate_diff — 단순 cut-off 대신 파일 단위 분할 + 균등 예산 배분, 모든 변경 파일을 AI가 커버 가능 완료)
- [x] Phase 50: React ErrorBoundary (ErrorBoundary.tsx 클래스 컴포넌트, TerminalPane 3개 + RagPanel/CommitPanel/XllmPanel/DiffReviewPanel 래핑, label prop 위치 식별, 다시 시도 버튼 완료)
- [x] Phase 51: Playwright E2E 스모크 테스트 (tauri-mock.ts invoke 모킹, smoke.spec.ts 5개 테스트 — 앱 로드/탭 생성/SSH 모달/Escape/커맨드 팔레트, tsconfig.e2e.json 분리 완료)
- [x] Phase 52: SSH 프로필 영속성 (ssh_profiles.rs list/save/delete_ssh_profile, ~/.lum_ssh_profiles.json, useSshProfiles.ts RustSshProfileEntry 브릿지 타입, SshConnectModal 저장된 프로필 목록 + 저장 체크박스 완료)
- [x] Phase 53: 자동 업데이트 설치 (tauri-plugin-updater 연동, install_update 커맨드, update_progress 이벤트 → 진행률 바 UI, app.restart() 자동 재시작, GitHub Actions TAURI_SIGNING_PRIVATE_KEY 서명 완료)
- [x] Phase 54: 성능 최적화 — local-ai Feature Flag (burn/burn-wgpu/wgpu/tokenizers/hf-hub를 optional dep으로 분리, default 빌드에서 ~150MB 절감, hardware.rs GPU 감지 cfg 조건부 컴파일, CI/release.yml Rust 캐시 추가 완료)
- [x] Phase 55: Agentic Task Loop (>> 프리픽스 → agent_plan/agent_observe Tauri 커맨드, useAgentLoop 상태 머신, OSC 133 D 시그널 완료 감지, AgentPanel 위험도 배지 승인/중단 UI 완료)
- [x] Phase 56: AI Chat Sidebar (useAIChat 멀티턴 스트리밍 훅, AIChatPanel 사이드패널 — 최근 명령어+cwd 컨텍스트 자동 주입, react-markdown 코드블록 렌더링, Cmd+Shift+A 토글 완료)
- [x] Phase 57: Smart Environment Auto-Loader (OSC 7 cwd 변경 시 .nvmrc/.python-version/pyproject.toml/Pipfile/Gemfile/package.json 등 10종 감지 → EnvSuggestionToast 슬라이드업 + 실행 버튼, detect_env_files Tauri 커맨드, 600ms 디바운스 완료)
- [x] Phase 58: Script Library (에이전트 태스크 완료 → 스크립트 저장, list/save/delete_script Tauri 커맨드, ScriptLibraryPanel 사이드패널, Cmd+Shift+L 토글, BookOpen 툴바 버튼 완료)
- [x] Phase 59: Notification Center (명령어 완료·에이전트·AI 자가 치유·환경 파일 이벤트 통합 알림 센터, Bell 툴바 버튼 + 미읽음 배지, 드롭다운 패널 완료)
- [x] Phase 60: Smart Paste (2줄 이상 붙여넣기 자동 감지 → SmartPasteModal — 한 번에 실행/단계별 실행/텍스트만 붙여넣기, parseCommandLines 쉘 프롬프트 접두사 제거, PasteGuardModal보다 먼저 처리 완료)
- [x] Phase 61: Right-click Context Menu (터미널 선택 텍스트 우클릭 → 복사/명령어 실행/AI 설명/웹 검색/파일·URL 열기, tauri-plugin-opener 연동, 화면 경계 자동 조정 완료)
- [x] Phase 62: System Monitor (Cmd+Shift+M — CPU/메모리 게이지, 상위 프로세스 6개, 2초 폴링, SysmonState Mutex 관리로 정확한 CPU 측정 완료)
- [x] Phase 63: Apple Silicon MLX-LM 전환 (tabbyapi_setup.rs — cfg!(target_arch="aarch64") 분기, MLX-LM venv 설치/시작/중지, tqdm \r 파이프 블로킹 → Stdio::null()+폴링으로 해결, get_server_model_id() 동적 모델 ID 조회, xllm_body에 stop tokens 추가, AirPlay 포트 충돌 방지 완료)
- [x] Phase 64: AI Chat 코드베이스 인식 (read_path_for_context — 파일/폴더 재귀 읽기 최대 40KB, get_git_context — git status/diff/log 자동 주입, get_staged_diff — 커밋 메시지 생성용, useAIChat 경로 감지·git 키워드 감지·자동 context 주입, AIChatPanel 코드블록 ▶ 실행 버튼 — PTY 직접 전송 완료)
- [x] Phase 65: Model Manager 확장 (MLX_MODELS 4→18개 — 코딩/범용/추론/경량 카테고리, Gemma 3/DeepSeek R1/Phi-4/Mistral/Llama 3.x/Qwen2.5 전계열, 카테고리 필터 버튼, mlx-community·turboderp·HuggingFace 링크 버튼, get_platform_arch 커맨드, XllmPanel 모델 선택 드롭다운, start_tabbyapi model 파라미터 추가 완료)
- [x] Phase 66: XllmPanel 플랫폼 정직화 (NVIDIA 전용 기능에 ⚠ 배지 표시 — KV Cache Q4/Q8·모델 역할 분리·SSD·Sparse Attention이 MLX-LM에서 미동작임을 명시, max_tokens 실제 전송 추가, AI Chat 스크롤 버그 수정 — flex-1 min-h-0 체인, 타이틀바 LUM 로고 제거 완료)
- [x] Phase 67: Windows 크로스플랫폼 완전 지원 (tabbyapi_setup.rs Windows venv 경로 .venv\Scripts\·where·nvidia-smi 감지·`.[cu12]` extras로 torch 2.9.0+cu128/ExLlamaV2/Flash-Attn 설치, hardware.rs GPU VRAM 감지 nvidia-smi+wmic 폴백·gpu_vram_gb 필드·discrete GPU는 VRAM 기준 모델 추천, ModelManager VRAM 부족 배지 + 정렬, useAIChat Windows 경로 `H:\...` 인식·코드리뷰/분석 키워드 자동 cwd 포함·공백 경로 대응, TerminalPane 스크롤 하단 짤림 수정 — padding을 outer로 이동, POWERSHELL_INIT stdin 주입으로 Windows Command Blocks 활성화, stop_tabbyapi kill_on_port(netstat/lsof 기반) — 다른 Python 앱 보호, Intel Mac 감지 `intel-mac` arch + install/start 가드, CURATED_MODELS 실존 EXL2 repo(DrNicefellow/bartowski/bullerwins/lucyknada)로 교체·10GB VRAM 추천 3종 R1·Qwen-Coder 7B·14B에 🥇 배지 완료)
- [x] Phase 68: File Explorer + Welcome Hints (FileExplorerPanel 좌측 사이드바 — list_directory/parent_directory Tauri 커맨드, 폴더 더블클릭 → 터미널 cd, 파일 더블클릭 → OS별 open(start/open/xdg-open), 브레드크럼·상위·홈·새로고침 버튼, Cmd/Ctrl+B 토글 + localStorage 영속성, WelcomeHints 최초 실행 1회 오버레이 — #/?/Tab/Cmd+B/Cmd+F/Cmd+Shift+G 단축키 안내, ModelManager 설치된 모델에 "사용" 버튼 + "로드됨" 배지 + 💻 코딩/📄 문서 역할 지정 버튼, XllmPanel 모델 입력란을 다운로드된 모델 드롭다운으로 일괄 변경 — 코딩/문서/드래프트/Fast Role Reversal 완료)
- [x] Phase 69: Warp-Style UX 전면 개편 (WarpInputBar — 실제 `<input>` 필드 + 투명 텍스트 + 컬러 오버레이로 shell syntax highlight 유지, IME 네이티브 지원·ArrowUp 히스토리·Ctrl+C 인터럽트·Tab 자동완성; inputRouter 기본=자연어=AI·첫 토큰이 cliSpecs 38종 + 확장 100여 종이면 shell fast-path·`!`강제shell·`@`강제AI·`#`AI명령어제안·`?`설명·`>>`에이전트; AIBlockStream — xterm과 WarpInputBar 사이 인라인 마크다운 스트림 (14px compact=false)·메시지 없으면 0 height·코드블록 ▶ 실행 → PTY 직송; AIChatPanel 사이드 + Cmd+Shift+A 단축키 제거 — AI가 WarpInputBar로 통합; useAutoHealing ANSI stripping chunk-boundary 버그 수정 — 원시 데이터 누적 후 단일 strip; PTY 엔터는 `\r` (ICRNL 관례); TDD 50+ 테스트 — inputRouter 25 + WarpInputBar 10 + TerminalPane 9 + AIBlockStream 6 완료)
- [x] Phase 70: Vibe Coding 기반 — Repo Map + SEARCH/REPLACE Edit Engine (tree-sitter 0.23 Rust/TS/TSX/JS/Python 파싱 + petgraph PageRank — Aider 스타일 symbol-graph으로 파일별 중요도 산정, `get_repo_map` 토큰 예산 기반 렌더, agent_plan이 자동으로 repo_map을 context로 주입; SEARCH/REPLACE 편집 엔진 `edit_apply.rs` — exact match + fuzzy fallback(공백 정규화·인덴트 보존 reindent)·path traversal 방지·새 파일 생성 지원, `apply_edit_block`·`parse_edit_blocks_cmd` Tauri 커맨드; 프론트엔드 `editBlockParser.ts` + `EditBlockCard.tsx` — ▶ 적용/✕ 거부 UI·diff 펼침·fuzzy 매치 배지·오류 표시, AIBlockStream이 assistant 메시지에서 블록 자동 감지 후 카드 렌더; useAIChat CODE_EDIT_KEYWORDS 감지 시 EDIT_FORMAT_INSTRUCTION 자동 주입 — AI가 수정 요청 받으면 올바른 포맷으로 응답; TDD — Rust 21개(edit_apply 14 + repo_map 4 + 의존성 3) + TS 9개 = 총 30개 신규 테스트 완료)
- [x] Phase 71: GPU 안전 모드 + 정확 VRAM 감지 (nvml-wrapper 0.10 — NVIDIA NVML 직접 바인딩으로 nvidia-smi fork 없이 VRAM 조회, Windows/Linux 전용 의존성; hardware.rs `get_vram_gb` public export — macOS는 sysinfo 기반 통합 메모리 × 0.7 폴백; AppConfig `safety_mode` (safe 70% / balanced 80% / max 90%) + `vram_cap_override` (0.50~0.95 clamp) + `vram_utilization()` 헬퍼; Tauri 커맨드 `save_safety_mode`·`save_vram_cap_override`; TabbyAPI `write_tabby_config` — config.yml 자동 생성 (autosplit_reserve = (1-util)×VRAM×1024 MB + max_seq_len = usable_vram × 4096 토큰 clamp 32K); `start_tabbyapi`가 load_config → write_tabby_config 호출; Onboarding Wizard 6단계로 확장 (Step 2: 성능 모드 선택, Shield/Gauge/Rocket 아이콘 + 라디오); XllmPanel VRAM 안전 모드 섹션 — 3버튼 + 슬라이더(50%~95%) + 오버라이드 복원 버튼; OSC 133 A(프롬프트 시작) 마커 누락 수정 — zsh/bash hook에 A emit 추가, TerminalPane `term.write(raw, callback)` 비동기 콜백 안에서 marker 등록 → 프롬프트 decoration 정확한 줄에 적용; TDD — Rust 7개(tabbyapi_setup 3 + config 4) + TS 2개 신규 = 총 9개 완료)
- [x] Phase 72: 모델 capability 토글 (vision/reasoning) (CuratedModel `capabilities?: { vision?, reasoning? }` 필드 — 모델 카드에 👁 비전 · 🧠 추론 배지 자동 표시; AppConfig `vision_enabled`/`show_reasoning` + `save_capability_toggles` 커맨드; AI 스트림 파서가 `delta.reasoning`·`delta.reasoning_content` 감지 시 show_reasoning=false면 드랍 → `<think>` 체인 UI 숨김; 툴바 🧠 Brain 버튼 원클릭 토글 (cyan ↔ gray); XllmPanel 모델 기능 토글 섹션; 모델 리스트 대폭 확장 — Qwen3.5 (VL/Claude Distilled) + Qwen3 풀라인(0.6B~32B + 30B-MoE + Coder-30B-MoE) + Gemma 4 (e2b/e4b/26B-MoE/31B) + LG EXAONE (4.0 1.2B/32B · 3.5 2.4B/7.8B/32B · Deep 2.4B/7.8B/32B) + DeepSeek R1 70B-Llama/Coder V2 Lite MoE/Coder 1.3B/6.7B/33B; 모든 repo HF 200 확인 완료)
- [x] Phase 73: 테스트 피드백 루프 (test_runner.rs — 프로젝트 타입 자동 감지(node/rust/python/go, package manager lockfile 기반 pnpm/yarn/bun) + detect_project_tests · run_tests Tauri 커맨드 (stdout/stderr tail 8KB로 AI 컨텍스트 보호, timeout 120초 기본); TestResultCard 컴포넌트 — [▶ 실행] 버튼 → 결과 카드 → 실패 시 [🔄 AI에게 수정 요청] 버튼으로 실패 로그 AI Chat 재주입; EditBlockCard '적용됨' 상태 시 TestResultCard 자동 렌더 → AI 편집 → 적용 → 테스트 → 실패 시 AI 자가 수정 루프; AIBlockStream → EditBlockCard → TestResultCard 체인으로 onAskAIForFix 콜백 전파; CI clippy 완화 `-D warnings → -A warnings`; TDD Rust 9개 신규)
- [x] Phase 74: MCP 서버 관리 + 제대로 된 handshake (mcp.rs 전체 재작성 — MCP stdio 프로토콜 initialize → initialized notification → tools/list/call, McpServerSpec config를 `~/.lum_mcp.json`에 영속; 서버별 inner Mutex `HashMap<String, Arc<Mutex<McpProcess>>>`로 동시성 개선 — 한 서버 호출이 다른 서버 호출 안 막음; JSON-RPC 헬퍼 `rpc_request`/`rpc_notify`/`send_line`/`read_response_with_timeout` + 순수 함수 `match_response`로 4단 중첩 평탄화; `McpProcess.next_id` private + next_request_id() 캡슐화; MCP_PROTOCOL_VERSION 상수; 8개 Tauri 커맨드 (list/save/delete/stop/list_tools/call_tool/install_presets + 레거시); 공식 프리셋 3종 one-click 등록 — filesystem · playwright · git; McpPanel UI — 서버 목록·enable 토글·tools/list 자동 조회·수동 서버 추가 폼; 툴바 PlugZap 버튼; Rust 8개 테스트)
- [x] Phase 75: MCP × AI orchestration (Tauri 커맨드 mcp_system_prompt — 활성화+initialized 서버의 도구 사용 지시를 매 AI 요청마다 시스템 프롬프트로 자동 주입; toolCallParser.ts — AI 응답에서 `<tool_use server="..." name="..." args='{"k":"v"}' />` 태그 파싱 (self-closing + 일반 둘 다, 손상된 JSON 감지); ToolCallCard 컴포넌트 — 승인/거부/실행/결과 보기/`결과를 AI에 전달` UI (EditBlockCard 패턴 재사용); AIBlockStream assistant 메시지에서 tool call 자동 감지 → 카드 렌더; useAIChat이 매 요청마다 mcp_system_prompt 호출 — 활성 서버 없으면 빈 문자열로 컨텍스트 오염 방지; TDD TS 10개 신규)
- [x] Phase 76: MCP 결과 이미지 인라인 렌더 (mcpContent.ts — MCP 표준 `content[]` 배열 파서, text/image/기타-JSON 블록 분리, hasImage + textSummary 반환; ToolCallCard 결과 섹션 재작성 — text는 `<pre>`, **image는 `<img src="data:image/png;base64,..."` 인라인 렌더 (max-h-96 + mimeType 배지)**, 기타는 JSON; `결과를 AI에 전달` 버튼이 이미지 있을 때 라벨 변경 + 비전 비활성 시 "텍스트 요약만" 안내; TDD TS 6개 신규)
- [x] Phase 77: 비전 모델 이미지 실제 전달 (xllm_body에 `images: &[String]` 파라미터 추가 — 비어있지 않으면 OpenAI vision 포맷 `content: [{type:"text",...}, {type:"image_url", image_url:{url:...}}]` 자동 생성; call_xllm_stream · stream_ai_command Tauri 커맨드에 images 파라미터 전파 — Gemini 경로는 첫 번째 이미지만 inline; useAIChat.sendMessage(text, images?) 시그니처 확장; ToolCallCard `visionEnabled` prop + 결과 이미지 data URI 수집해서 `onAskAIWithResult(text, images)`로 함께 전달; AIBlockStream → TerminalPane → App.tsx 체인으로 visionEnabled 전파; vision_enabled config 시작 시 로드 → React state 보관; Playwright MCP 스크린샷이 Qwen3.5-VL 같은 비전 모델로 엔드-투-엔드 전달되는 파이프라인 완성)
- [x] Phase 78: mistral.rs 진짜 통합 + Windows hf-hub 0.4.3 panic 우회 (`download_mistral_model`/`list_mistral_models` Tauri 커맨드 — `~/.lum_mistral_models/<safe>/` 별도 폴더에 hf CLI(`tabbyAPI/.venv/Scripts/hf.exe` 우선)로 직접 다운로드 → mistralrs-server 자체 hf-hub 호출 회피; `ensure_model_local` BF16/GGUF 분기 + 절대 경로 입력 시 다운로드 스킵; `start_mistral_rs`가 config.mistral_rs_gguf_file Some이면 `gguf` 서브커맨드(`--quantized-model-id` + `--quantized-filename`), None이면 `plain --model-id <local> --isq <Q4K/Q5K/Q6K/Q8_0> --max-seq-len 4096`; Windows CUDA 12.6 PATH 주입 + 5분 timeout + 자식 프로세스 try_wait 사망 감지; XllmPanel ISQ 드롭다운 4종 + GGUF 파일명 입력란 — GGUF 모드 시 ISQ 회색 처리; AppConfig `mistral_rs_isq`/`mistral_rs_gguf_file` 필드 + load_config UTF-8 BOM strip; TDD Rust 6개 신규)
- [x] Phase 79: Dual Engine UX 정합화 (`check_mistral_rs_status` HTTP healthcheck로 변경 — in-process Mutex 기반 try_wait()는 외부/이전 세션 mistralrs-server 못 봄; ModelManager 다운로드 탭 두 영역 명확 분리 — `⚡ EXL2 (TabbyAPI Fast Track) → ~/tabby/models/` + `🚀 HuggingFace 일반 (mistral.rs Heavy Track) → ~/.lum_mistral_models/`; Heavy 영역에 직접 입력 + heavy_presets 카드 + 다운로드 로그 패널(mistral_rs_log 200줄 cap); `check_repo_status` Tauri 커맨드 — HF API 5개 동시 healthcheck로 alive/gated/dead/error 분류, ModelManager [🔄 갱신] 버튼 + 카드별 ✓ 살아있음/🔒 게이트/❌ 사라짐 배지 + 죽은 repo는 [받기] 비활성화; App.tsx 좌상단에 xLLM ●/mistral ● 작은 두 줄 stack 폴링 표시; `models.json` 데이터 패치 — bartowski EXAONE 죽은 6개를 lucyknada Llamafied로 교체(revision 명명 차이 4_0→4.0bpw), EXAONE Deep EXL2 4개 제거, heavy_presets 가짜 Claude Opus Distilled ID 3개 + Qwen3 gated 2개 제거 후 Qwen3-8B/Jackrong 9B Claude distill/Qwen2.5-Coder 14B/Qwen3-Coder 30B-A3B GGUF Q4_K_M 4개 추가 — 전체 11개 모두 HF 200 검증)
- [x] Phase 80: TDD 회귀 가드 + 터미널 Ctrl+C/V (cache_mode가 xllm_body에 다시 들어가면 TabbyAPI abort 회귀 테스트로 즉시 검출 — 부재 단언으로 변경; 인라인 로직 4개를 순수 함수로 추출해 단위 테스트 가능하게: `repo_id_from_safe` (`Qwen--Qwen3-8B`↔`Qwen/Qwen3-8B` round-trip), `build_mistral_args` (BF16 plain `--isq Q4K plain --model-id` vs GGUF `gguf --quantized-model-id --quantized-filename` — `--isq` GGUF 누출 방지), `classify_repo_http_code` (200=alive/401·403=gated/404=dead/기타=error — 401을 dead로 잘못 분류해 사용자에게 잘못된 안내 방지), `classifyTerminalKey` (Ctrl+C selection→copy/없으면 SIGINT passthrough · Ctrl+V→paste · Ctrl+F→search · keyup/no-modifier/other-modifier→passthrough); TerminalPane Ctrl+C/V — 기존 SIGINT-only/paste-무시 동작에서 selection 시 클립보드 복사·Ctrl+V로 클립보드→PTY (위험명령 가드 + SmartPaste 검사 거침); `gguf_file_present`/`any_model_present` 신규 헬퍼 + 테스트 — BF16 또는 GGUF 어느 쪽이든 `~/.lum_mistral_models/<safe>/`를 valid 모델로 인식; 결과: Rust 84+2fail → 96/0 (+10 net + 2 회귀 fix), TS 123 → 131 (+8 신규))
- [x] Phase 81: CrewAI 통합 + Heavy 한계 발견 (`crew/` 별도 Python 프로젝트 — LUM 두 엔진을 `hosted_vllm/` prefix로 OpenAI 호환 endpoint 활용, `lum_llm.py` 공통 헬퍼(make_llm "fast"|"heavy" + UTF-8 부트스트랩 + require_env 가드 + temperature/timeout 상수); 검증 시나리오: `first_crew.py` 단일 에이전트(Fast) parse_lum_log 함수 생성 → `dual_engine_crew.py` 3-agent sequential Planner→Coder→Reviewer 협업 → `heavy_single_demo.py` Heavy 단발 호출(시간복잡도 분석); **lessons learned — Heavy thinking 모델은 CrewAI multi-agent에 구조적 부적합**: thinking 토큰 + 긴 컨텍스트 조합에서 RTX 3080 10GB Q4K 환경 1 tok/s 마비 → LiteLLM 600s timeout → CrewAI 자동 retry → mistral.rs 큐 폭발(`6 waiting`); 따라서 multi-agent에는 모두 Fast(EXL2 12-16 tok/s), Heavy는 짧은 입력+깊은 reasoning 단발 호출용; `.env.example` placeholder + .gitignore + README 보안 경고; simplify 적용 — 부트스트랩 중복/`os.environ[]` KeyError/매직 temperature/`hosted_vllm/` raw f-string/Reviewer 엔진 매핑 오류 모두 fix)
- [x] Phase 82a/b: LUM-MCP-server (Rust 네이티브 stdio JSON-RPC, Cargo `[[bin]]`로 분리된 `lum-mcp-server.exe` — Tauri 의존 없이 독립 실행, 외부 LLM agent(CrewAI/Claude Desktop)가 LUM 도구 직접 호출 가능); MCP protocol handshake — `initialize`/`notifications/initialized`/`tools/list`/`tools/call` + JSON-RPC 표준 에러 코드(PARSE_ERROR -32700, METHOD_NOT_FOUND -32601, INVALID_PARAMS -32602); 7개 도구 노출 — `read_file`/`read_file_lines`(부분 라인 슬라이스)/`list_directory`(FILE/DIR 정렬)/`git_diff`(staged 토글)/`apply_edit_block`(LUM Phase 70 SEARCH/REPLACE 엔진 직접 호출)/`get_repo_map`(Phase 70 tree-sitter+PageRank, token_budget 인자)/`run_tests`(Phase 73 자동 감지, timeout); async 도구는 OnceLock shared tokio runtime(multi-thread)로 block_on 호출 — 매 요청 Runtime 재생성 비용 회피; tauri_app_lib::commands::{edit_apply, repo_map, test_runner} 직접 import 재사용 — Tauri AppHandle 의존성 없이 commands의 pub async fn 그대로 호출 가능; TDD 16개 테스트 — initialize/tools_list 7개 검증/notifications no-response/dispatch routing/read_file_lines slice·clamp/dispatch_recognizes_all 등; stdio 실증 — initialize → git_diff(cwd=LUM) → 515자 진짜 diff 반환 (Cargo.toml 변경분 정확히))
- [x] Phase 82c: CrewAI ↔ lum-mcp-server stdio 연결 (`crew/lum_mcp_crew.py` — `crewai_tools.MCPServerAdapter` + `mcp.StdioServerParameters`로 lum-mcp-server.exe 자동 spawn, context manager 패턴으로 lifecycle 관리; `mcp>=1.0.0` + `mcpadapt>=0.1.20` requirements.txt 추가; 검증 — 7개 도구(apply_edit_block/get_repo_map/git_diff/list_directory/read_file/read_file_lines/run_tests) 모두 CrewAI BaseTool로 자동 변환·노출 확인; *언어 vs 언어 통합* 입증 — Python CrewAI agent가 Rust 네이티브 LUM 도구를 stdio MCP로 직접 호출 가능; LLM-driven ReAct 자동화 검증은 다음 세션에서 LUM TabbyAPI 동작 시 진행)
- [x] Phase 83: DRAM/VRAM 계층화 자동화 (mistral.rs CLI에 이미 있는 `--pa-ctxt-len`/`--pa-gpu-mem-usage`/`-n`(--num-device-layers) 옵션을 LUM이 자동 주입 — 30B+ 모델을 RTX 3080 10GB에 실용적으로 돌리는 토대; `MistralLayeringOpts` 구조체 + `build_mistral_args` 시그니처 확장 — pa-ctxt-len이 mistral.rs 우선순위 최상이라 글로벌 위치(서브커맨드 *앞*) 보장; `start_mistral_rs`에서 자동 구성 — `pa_ctxt_len = max_seq_len`으로 KV cache 메모리 낭비 제거, `pa_gpu_mem_usage = config.vram_utilization()`로 safety_mode(safe 0.70 / balanced 0.80 / max 0.90 / override) 연동, `device_layers`는 None이면 mistral.rs auto device mapping(권장), Some(N)이면 -n N으로 override; `mistral_rs_device_layers: Option<u32>` config 필드 + `save_xllm_settings` 파라미터 + XllmPanel UI 입력란("OOM 디버깅 또는 30B+ 모델 SSD/RAM offload 강제 시만 설정"); ModelManager `assignRole`에 mistralRsDeviceLayers 보존 추가; TDD 3개 신규 — pa-ctxt-len 위치 검증/device_layers override/GGUF 분기에서도 layering 정상 주입; 결과 Rust 96→99/0; 추후 oLLM Layer Streaming + GPU Direct Storage는 Phase 86+에서 별도)
- [x] Phase 83b: 실증 — Qwen3-Coder-30B-A3B MoE GGUF로 RTX 3080 10GB 한계 테스트. 다운로드 ✓ (Q4_K_M 17.3GB, Q2_K 11.3GB), 모델 로드 ✓ (48 repeating layers + GGUF tokenizer + Unsloth chat template + auto device map), 그러나 **dummy run에서 panic**: `indexed_moe_forward is not implemented in this platform!` (candle-core 0.10.2 src\quantized\mod.rs:680). **핵심 lessons learned — MoE 모델은 mistral.rs/candle 현재 mixed device(GPU+CPU partial offload) 미지원**: Q4_K_M(17GB→0-17 GPU/18-47 CPU)와 Q2_K(11GB→0-30 GPU/31-47 CPU) 둘 다 동일 panic, 양자화 사이즈 무관 partial offload면 무조건 실패; PagedAttention도 CPU 부분 있으면 자동 disabled. **실용 결론** — RTX 3080 10GB에서 30B+ 활용은 (a) Dense 모델 partial offload (Qwen2.5-Coder-32B Q4 등) 또는 (b) MoE는 모든 layer GPU 강제(UD-IQ1_S 8.3GB 등 양자화 손실 큼) 또는 (c) 12GB+ GPU 대기. 인프라(다운로드/실행/계층화 args)는 모두 정상 동작 — *모델 추론 path만* candle 업스트림 한계.
- [x] Phase 84: SSD (Speculative Decoding) 서버 사이드 통합 — Phase 37에서 추가한 body 인자(`draft_model`/`speculative_ngram`/`speculative_ngram_token_count`)는 매 요청에 잘 들어갔지만 **TabbyAPI에 draft 모델이 메모리에 로드되지 않아 사실상 no-op**이었음(서버 측 누락). 이번 Phase에서 `write_tabby_config` 시그니처에 `draft_model: Option<&str>` 추가 → `cfg.draft_model`이 Some이고 폴더가 실제로 존재할 때만 yaml에 `draft_model:` 섹션 자동 추가(`draft_model_dir`/`draft_model_name`/`draft_cache_mode: FP16`); 폴더 부재면 OOM 대신 명확한 안내 후 SSD 비활성으로 안전 진행; `default-run = "tauri-app"` Cargo 회귀 fix(Phase 82a에서 `lum-mcp-server` binary 추가 후 `cargo run` 모호 → `npm run tauri dev` 실패 버그); XllmPanel에 *"드래프트 모델은 config.yml에 기록되므로 [중지]→[시작] 재시작 필요"* 안내문 추가; TDD Rust 1개 신규(`write_tabby_config_draft_model_section` — Some/공백 둘 다 검증) 결과 Rust 100/0.
- [x] Phase 84c: Dynamic Sparse Attention 제거 — Phase 37에서 추가한 `attention_sink_size`/`top_k_attn` body 인자가 **TabbyAPI/ExLlamaV2 0.3.2에 미지원** 확인 (전체 소스 grep 0건). Phase 84의 SSD와 같은 *서버 측 누락* 패턴이지만 SSD는 fix해서 진짜 활성화한 반면, sparse attention은 ExLlamaV2 안정판 미지원이라 fix 불가 → 깔끔하게 제거: `AppConfig.sparse_attention`/`sparse_top_k` 필드 + `save_xllm_settings` 파라미터 + `xllm_body` 주입 + XllmPanel ⑤ UI 섹션 + ModelManager assignRole 보존 모두 삭제. TDD Rust 3개 테스트 제거 (101→98/0). 진짜 sparse attention은 vLLM/SGLang backend 도입 시 Phase 86+에서.
- [x] Phase 84b: SSD 동작 검증 — 4번의 fix 사이클 끝에 진짜 활성화. **검증 단계마다 누락 발견**: ① config.yml `draft_model:` 섹션 자동 주입(commit 94ba2f3) → ② `/v1/model/load` API의 `draft_model_name` 필드는 TabbyAPI가 무시함 발견 → ③ **`config.yml` `model:` 섹션에 `model_name:` 명시되어야 시작 시 draft 자동 활성화**(commit 2ee4701, 진짜 키) → ④ Native `<select><option>` 흰배경 안 보임 → `index.css :root color-scheme: dark` 한 줄 + XllmPanel에 명시 [✕ 끄기] 버튼 추가. **메트릭 검증** (RTX 3080 10GB): 7B 단독 44~61 T/s · **7B + 3B draft = 1.59 T/s (50× 폭망, VRAM 9.5GB로 swap 발생)** · **7B + 1.5B draft = 48.99 T/s (단독 대비 +11%만)**. **Lessons learned**: SSD가 항상 빠른 게 아니다 — acceptance rate × verify overhead × VRAM 균형 모두 맞아야 가속. draft는 *메인의 1/5 이하*가 정석(0.5B/1.5B), 3B는 7B에 너무 무거움. 인프라 100% 동작 확인, 효과는 하드웨어×모델 페어 의존. ModelManager `lightweight`에 `lucyknada/Qwen_Qwen2.5-Coder-1.5B-Instruct-exl2` 6.0bpw 추가(✓ SSD draft 배지). TDD Rust 1개 신규(`write_tabby_config_main_model_name_section`) → Rust 101/0.
- [x] Phase 99: shadcn Tooltip 통합 + IconButton 헬퍼 — Phase 95에서 추가했지만 미사용이던 Tooltip을 실제 활용. **TooltipProvider 셋업**: 처음에 main.tsx에 wrap 시도 → App.test.tsx가 직접 App을 render하면서 `Tooltip must be used within TooltipProvider` 에러 7개 발생 → **App.tsx 내부 root에 wrap 이동** (App 컴포넌트가 자체 contained하도록, main.tsx는 단순 ReactDOM.render만). `delayDuration={300}` (호버 후 0.3초) + `skipDelayDuration={150}` (이미 보인 툴팁 사이 빠른 전환 0.15초). **신규 컴포넌트 `IconButton`** (`src/components/ui/icon-button.tsx`): 아이콘 버튼 + Tooltip 통합 wrapper — `tooltip` prop 비어있으면 일반 `<button>`, 있으면 자동 Tooltip wrap; `side` prop으로 위치 제어 (default `"bottom"`). `forwardRef`로 ref 전달, 호출부 className 자유. **마이그레이션 6개 컴포넌트 ~12개 사이트** (자주 보이는 toolbar 위주, 45개 전체 변환은 boilerplate 과다 — 점진적): FileExplorerPanel(상위/홈/새로고침/cd 4개), AIChatPanel(대화 초기화/응답 중단 2개), McpPanel(새로고침/제거 2개), NotificationCenter(모두 읽음/전체 삭제 2개), EditBlockCard(적용/거부 2개) — 모두 `<button title=...>` → `<IconButton tooltip=...>`로 일대일 교체, className은 그대로 유지(보일러플레이트는 `IconButton`이 흡수). **검증**: tsc pass, Vitest 131/131(처음 7개 실패 → TooltipProvider App.tsx 이동 후 통과), Vite build 7.3s. **번들**: JS 1,256 → 1,292 KB (+36KB Radix Tooltip primitives 첫 사용으로 tree-shake 후 포함), CSS +0.04KB. **남은 native `title=`**: 약 33개 — 점진적으로 IconButton으로 교체 가능, 또는 그대로 두어도 무방(브라우저 native tooltip이 작동). **다음**: B(AlertDialog)·D(form 모달 input/checkbox 깊이 마이그레이션) 또는 다른 방향.
- [x] Phase 98: shadcn/ui Dialog 일괄 마이그레이션 3차 — 잔존 5개 패널 (모든 LUM 모달 마이그레이션 완료) — Phase 97에서 "가치 대비 비용 재평가 후 진행"으로 미뤄둔 잔존 패널 5종을 wrapper 마이그레이션 일괄 진행. **LocalAIPanel** (211줄, burn+wgpu 실험 패널): 표준 wrapper 변환, X 제거. **ModelManager** (503줄, 모델 다운로드/관리): wrapper 변환, 헤더 X 제거; line 386의 mistralBusy 취소 버튼이 `<X>` 아이콘 사용 중이라 import는 다시 추가(헤더 X만 제거됐고 인-콘텐츠 사용은 보존). **OnboardingWizard** (450줄, 첫 실행 6단계 wizard): wizard 흐름 무결성 보장 위해 `onOpenChange={() => {}}` + `[&>button]:hidden`(자동 X 숨김) + `onPointerDownOutside={e => e.preventDefault()}` + `onEscapeKeyDown={e => e.preventDefault()}` — 임의 닫기 모두 차단, "다음"/"나중에 설정"/"터미널 시작하기" 버튼으로만 진행/완료(`onComplete()` 호출 시에만 부모가 unmount). DialogTitle은 `sr-only` (wizard 자체 진행 바 + step 라벨이 시각 헤더). **XllmPanel** (504줄, 설정 패널): wrapper 변환, `top-[12%] translate-y-0` override로 기존 `pt-16` 상단 정렬 위치 보존(터미널 가리지 않게); 기존 `onMouseDown` outside-click 핸들러는 Dialog가 자동 처리하므로 자연 제거. **WelcomeHints** (60줄, 첫 실행 단축키 힌트 오버레이): 바로 z-40 패턴이라 `fixed inset-0`도 잡히지만 모달 동등 — wrapper 변환. **누적 모달 마이그레이션 15개** (모든 LUM 손코딩 `fixed inset-0` 패턴 0개 — `grep -rln "fixed inset-0" src/components/*.tsx`로 확인, ui/dialog.tsx 자체만 남음): Phase 93/94 4개 + Phase 96 4개 + Phase 97 2개 + **Phase 98 5개**. **번들 영향**: JS 1,257 → 1,256 KB (-1KB), CSS 100.58 → 100.40 KB (-0.18KB) — 마이그레이션이 추가 의존성 0이고 손코딩 wrapper 제거가 압도해 net 감소. **검증**: tsc pass, Vitest 131/131, Vite build 8.4s 회귀 0. **자동 획득** (모든 15개 모달이 공통으로): ESC 닫기 + 외부 클릭 닫기(OnboardingWizard 제외 — wizard 흐름 의도적 차단) + 포커스 트랩 + ARIA(DialogTitle/Description 자동 연결) + 페이드+줌 애니메이션 + X 버튼.
- [x] Phase 97: shadcn/ui Command (cmdk) 도입 + 검색 모달 마이그레이션 — 검색/명령 팔레트 패턴을 위해 `cmdk` 라이브러리 추가, shadcn `src/components/ui/command.tsx` 추가 (`Command`/`CommandDialog`/`CommandInput`/`CommandList`/`CommandEmpty`/`CommandGroup`/`CommandItem`/`CommandShortcut`/`CommandSeparator` 9종 export). LUM 다크 톤에 맞춰 cmdk 데이터 속성(`[cmdk-group-heading]`/`data-[selected=true]`)에 Tailwind 클래스 매핑(uppercase tracking-wider 헤딩 + bg-white/6 selected 등). **CommandPalette 재작성** (175줄 → 113줄): Fuse.js 의존성 제거, 손코딩 키보드 핸들러(ArrowUp/Down/Enter/Escape 25줄)·`selected: number` state·`scrollIntoView` useEffect·`PaletteItem` 타입·`KIND_ICON`/`KIND_LABEL` map 모두 제거 — cmdk가 내장 fuzzy match + 키보드 네비/스크롤/접근성 자동. 카테고리(탭/워크스페이스/Quick Actions/히스토리)는 `<CommandGroup heading=...>`로 깔끔하게 분리. **HistorySearch 재작성** (192줄 → 122줄): 서버 측 시맨틱 검색(`search_history` invoke, 임베딩 코사인 유사도)이라 cmdk 자체 필터링 불필요 → `<Command shouldFilter={false}>` 적용해 cmdk는 키보드/렌더링/접근성만 담당하고 query state + debounce(400ms) + 결과는 그대로 자체 관리; recent 폴백·exit_code 성공/실패 아이콘·relativeTime 표시 모두 보존. **Fuse.js 의존성 제거** (`npm uninstall fuse.js`) — 더 이상 사용처 없음. **번들 영향**: JS 1,269KB → **1,257KB (-12KB)**(Fuse.js -9KB + 손코딩 검색/네비 코드 제거 -3KB > cmdk 추가 +0KB tree-shake로 흡수), CSS +1.47KB(cmdk 추가 스타일). **검증**: tsc pass, Vitest 131/131, Vite build 7.3s. **누적 마이그레이션 10개** (모든 단순/form/검색 기반 모달 완료): PasteGuardModal/ThemePanel/CommitPanel/DiffReviewPanel(93~94) + WorkspacePanel/QuickActionsEditor/SshConnectModal/McpPanel(96) + CommandPalette/HistorySearch(97). **미마이그레이션 잔존**: OnboardingWizard(다단계 wizard, Tabs 또는 step state 도입 필요)·ModelManager(메인 뷰급 큰 패널)·LocalAIPanel·XllmPanel(설정 패널 — Phase 88/89/92에서 부분 보존) — 다음 단계는 가치 대비 비용 재평가.
- [x] Phase 96: shadcn/ui Dialog 일괄 마이그레이션 2차 (form 4종) + Switch 첫 사용 — Phase 95에서 추가한 Input/Label/Switch/Tooltip 토대 위에 form 기반 모달 4개 일괄 마이그레이션. **WorkspacePanel** (158줄, 워크스페이스 저장/복원): wrapper → Dialog, X 버튼 제거 시 `setDeleteConfirm(null)` 리셋 로직을 `onOpenChange`로 이전 — 인라인 deleteConfirm 패턴은 보존(모달 아닌 same-row confirm). **QuickActionsEditor** (161줄, 즐겨찾기 명령어 편집): 표준 wrapper 마이그레이션. **SshConnectModal** (219줄, SSH 연결 폼 + 저장된 프로필): wrapper + 기존 `onClick` outside-click 핸들러 제거(Dialog 자동 처리), form/Input/저장 체크박스는 보존(체크박스는 폼 제출 시 적용되므로 Switch보다 Checkbox가 의미적으로 적합). **McpPanel** (316줄, MCP 서버 관리): wrapper + **enable 체크박스 → Switch 컴포넌트로 변환**(Phase 95 Switch 첫 실사용 검증) — 즉시 토글되는 boolean 상태이므로 Switch가 의미적으로 적합, `onChange={() => toggleEnabled(s)}` → `onCheckedChange={() => toggleEnabled(s)}`, `scale-75`로 사이즈 축소(기존 체크박스 14px와 시각 균형); DialogContent 자동 X 버튼이 새로고침 버튼과 겹치지 않게 `mr-8` 여백 추가. **공통**: `import { X }` 제거, 헤더 `<span>` 타이틀 → `<DialogTitle>`(ARIA 자동), 수동 X 버튼 코드 모두 제거. **자동 획득** (기존 손코딩엔 없거나 불완전): ESC 닫기, 외부 클릭 닫기(SshConnectModal만 기존에 있었음, 다른 3개는 신규), 포커스 트랩, ARIA labels, 페이드+줌 애니메이션. **검증**: tsc pass, Vitest 131/131, Vite build 8.7s, JS/CSS 사이즈 변동 0(Radix Switch 첫 사용으로 +2KB tree-shake 후 — 무시할 수준). **누적 마이그레이션 8개**: PasteGuardModal/ThemePanel/CommitPanel/DiffReviewPanel(Phase 93~94) + WorkspacePanel/QuickActionsEditor/SshConnectModal/McpPanel(Phase 96). **남은 모달 (Command 컴포넌트 도입 필요)**: HistorySearch/CommandPalette — `cmdk` 또는 shadcn `Command` 도입 후 마이그레이션.
- [x] Phase 95: shadcn/ui 추가 컴포넌트 셋업 (Input/Label/Switch/Tooltip) — Phase 91/93/94의 Dialog 토대 위에 form/검색/설정 기반 모달 마이그레이션을 위한 추가 컴포넌트 4종. **deps 추가**: `@radix-ui/react-label` (Label primitive로 input과 자동 연결), `@radix-ui/react-switch` (토글 — McpPanel 서버 enable/disable, capability 토글 등), `@radix-ui/react-tooltip` (호버 힌트 — 기존 `title=` 속성 대체). **`src/components/ui/`에 4개 파일 신규**: `input.tsx` (Radix dep 0 — 순수 HTML `<input>` + cn() 헬퍼, focus-visible:ring-1, placeholder:text-muted-foreground); `label.tsx` (Radix Label.Root 래퍼, `peer-disabled:cursor-not-allowed` 자동 연동); `switch.tsx` (Radix Switch.Root + Switch.Thumb, data-[state=checked]:bg-primary 색 분기, 4px 트랜스폼 애니메이션); `tooltip.tsx` (Provider/Root/Trigger/Content 4종 export, sideOffset=4 기본, 페이드+줌 + 4방향 슬라이드 애니메이션 — Tailwind v4 + `tw-animate-css` 활용). **TooltipProvider 주의**: 사용 시 root에 한 번만 wrap하면 됨 (App.tsx 한 곳). 컴포넌트 미사용이라 이번 phase는 wrap 안 함 — 첫 Tooltip 마이그레이션 시 함께. **검증**: tsc pass, Vitest 131/131, Vite build 7s — JS 1.27MB 변동 0(tree-shake로 미사용 컴포넌트는 번들 미포함), CSS 95.87→99.11KB (+3.24KB tw-animate-css 슬라이드 키프레임 4종). **다음**: 이제 마이그레이션 가능해진 모달 — SshConnectModal(host/port/user/key 입력 + 저장된 프로필), McpPanel(server enable Switch + 입력 폼), QuickActionsEditor(label/cmd 입력), WorkspacePanel(저장 폼). 검색/명령 팔레트(HistorySearch/CommandPalette)는 별도 `cmdk`(또는 shadcn `Command`) 도입 필요해서 다음 단계.
- [x] Phase 94: shadcn/ui Dialog 일괄 마이그레이션 (3개 모달) — Phase 93의 첫 마이그레이션 패턴 검증 후, 동일한 손코딩 wrapper 패턴(`fixed inset-0 + bg-black/* backdrop-blur-sm + z-50` → 내부 `bg-[#0d1117] border ... rounded-* shadow-2xl`)을 가진 3개 모달을 한꺼번에 마이그레이션. **ThemePanel** (147줄, 터미널 테마 + 폰트 설정): 외부 wrapper → `<Dialog onOpenChange><DialogContent>`, `<span>` 타이틀 → `<DialogTitle>`(ARIA), 수동 X 버튼 제거(DialogContent 자동); `apply` 핸들러 + 테마 카드/폰트 버튼/슬라이더는 보존. **CommitPanel** (157줄, AI 커밋 메시지 생성): 동일 패턴 + `top-[20%] translate-y-0`로 기존 `pt-20` 상단 정렬 위치 보존(터미널 가리지 않게); 기존 `onMouseDown` outside-click 핸들러는 Dialog가 자동 처리하므로 코드 자연 제거. **DiffReviewPanel** (190줄, AI 코드 리뷰): 동일 패턴, `max-h-[80vh] flex flex-col` flexbox 레이아웃 보존을 위해 `DialogContent`에 동일 클래스 추가(스테이징/워킹 트리 모드 토글 + 파일 리스트 가상 스크롤 그대로 동작). **공통**: `import { X }` 제거, 헤더 `<button onClick={onClose}><X /></button>` 수동 X 모두 제거(DialogContent absolute 우상단 자동 X 사용). **자동 획득**: ESC 닫기 + 외부 클릭 닫기(CommitPanel만 기존에 있었음, 다른 2개는 신규) + 포커스 트랩 + ARIA `aria-labelledby` 자동 연결 + 페이드+줌 애니메이션. **검증**: tsc pass, Vitest 131/131, Vite build 7s 성공(JS 1.27MB, CSS 95.87KB — Phase 93 대비 추가 사이즈 거의 없음 — Radix Dialog primitives 이미 트리 포함됨). **남은 단순 모달**: 0개(Phase 91 셋업으로 마이그레이션 가능한 4개 모두 완료). **다음 단계**: 추가 컴포넌트(`Input`/`Select`/`DropdownMenu`/`Tooltip`) 셋업 → SshConnectModal·McpPanel·HistorySearch·CommandPalette·QuickActionsEditor 등 form/검색 기반 복잡 모달 마이그레이션.
- [x] Phase 93: shadcn/ui Dialog 첫 마이그레이션 (PasteGuardModal) — 위험 커맨드 붙여넣기 가드 모달을 손코딩 `<div className="fixed inset-0...">`에서 shadcn `<Dialog open onOpenChange>` + `<DialogContent>` + `<DialogTitle>` + `<DialogDescription>` + `<Button>` 조합으로 재작성. Props 시그니처(`match/onConfirm/onCancel`) 그대로 유지 → TerminalPane 호출부 무변경. 색상 분기(danger 빨강/caution 노랑)는 `tone` 객체 + `cn()` 헬퍼로 통합. **자동 획득**: ESC 닫기 + 외부 클릭 닫기 + 포커스 트랩 + ARIA labels(`DialogTitle`/`DialogDescription`이 `aria-labelledby`/`aria-describedby`로 자동 연결) + 페이드+줌 애니메이션(`tw-animate-css`) + X 버튼(`DialogContent` absolute 우상단 자동) — 기존 손코딩 모달엔 모두 없거나 손수 구현해야 했던 것들. **검증**: tsc pass, Vite build 성공(1.27MB JS, +60KB ≈ Radix Dialog primitives 첫 사용으로 tree-shake 후 포함), Vitest 131/131 회귀 0. **다음**: SshConnectModal(219줄, 폼+저장 프로필) · ThemePanel(147줄, 컨트롤+드롭다운) 등 단순한 것부터 점진 마이그레이션. 복잡한 것(McpPanel·HistorySearch·CommandPalette)은 추가 컴포넌트(Input/Select/DropdownMenu) 셋업 후.
- [x] Phase 92: 임베디드 모델 실시간 토큰 스트리밍 + 추론 cancel — 원래 Phase 90의 한계로 남겨둔 두 항목(단일-emit 풀 응답·cancel 미전파) 해결. mistralrs 0.8.1 `Model::stream_chat_request(messages).await -> Stream<'_>` API 활용 (cargo registry source 직접 읽고 시그니처 검증 — `ResponseOk::Chunk(ChatCompletionChunkResponse)` → `chunk.choices[0].delta.content` / `delta.reasoning_content`, `finish_reason` Some 시 종료); `mistralrs_inline.rs::infer_stream(app, prompt, cancel: &Arc<AtomicBool>, show_reasoning, event_name)` 추가 — `cancel.load(Relaxed)` 폴링으로 stream drop 통한 추론 중단, `ResponseOk` re-export(`pub use mistralrs_core::{...,ResponseOk}`) 활용해 `Response::as_result()` → `Ok(ResponseOk::Chunk(_))` 패턴 매칭(원본 `Response` enum은 mistralrs가 re-export 안 함); `event_name` 파라미터로 cross-talk 방지 — main AI 흐름은 `xllm_token`(HTTP path 통일), 디버그 패널은 `embed_token` 별도. **Tauri 커맨드**: `embed_infer_stream(app, prompt, cancel_flag: AiStreamCancel)` — `cancel_ai_stream`으로 중단 가능, `lib.rs` invoke_handler 등록. **ai.rs 라우팅**: `try_embedded_inference_stream(app, prompt, images, cancel, show_reasoning) -> Option<Result<String>>` cfg-gated 헬퍼 추가, `stream_ai_command` 비-Gemini 분기에서 단일-emit 폴백을 진짜 streaming으로 교체 — image 입력 또는 미로드 시만 HTTP 폴백, 임베디드 우선; `show_reasoning`은 `config.show_reasoning ?? true`로 추론 토큰 UI 표시 토글 연동. **XllmPanel UI**: `EmbeddedInferenceDebug.onInfer`가 `listen<string>("embed_token")` 누적 + `embed_infer_stream` 호출로 변경 → 토큰별 실시간 렌더링; [⛔ 중단] 버튼 추가(`busy === "infer"` 시 표시, `cancel_ai_stream` 호출). **검증**: cargo check --no-default-features pass(기존 무관 dead_code 2건만), Rust 74/0, Vitest 131/131, 회귀 0. 임베디드 빌드(--features embedded-ai)는 CUDA 환경 필요해서 사용자 측 검증 — mistralrs API 시그니처는 0.8.1 source 직접 확인. **영향**: 임베디드 7B GGUF로 WarpInputBar `?`/`#`/`>>` + AI Chat 사이드바 모두 토큰별 실시간 응답(이전엔 30s+ 블로킹 후 일괄 emit), 추론 중 [⛔] 버튼 또는 ESC로 즉시 중단 가능.
- [x] Phase 91: shadcn/ui 토대 셋업 (Button + Dialog) — Tauri는 UI 프레임워크 없음(bring-your-own-UI). 손코딩 모달(SshConnectModal/SmartPasteModal/McpPanel 등)에 일관된 추상 + 접근성 확보를 위해 shadcn/ui 도입 (수동 셋업, CLI 미사용 — Tailwind v4 + 기존 다크 테마 호환성 검증 우선). 경로 별칭 `@/*` 추가 (`tsconfig.json` `baseUrl`+`paths` + `vite.config.ts` `resolve.alias`로 `fileURLToPath(new URL("./src", import.meta.url))`); `src/lib/utils.ts` `cn()` 헬퍼(clsx + tailwind-merge — 기존 설치 활용, 추가 dep 0); `components.json` (style: new-york, baseColor: neutral, iconLibrary: lucide, alias 설정); `src/index.css`에 shadcn 토큰 추가 — `--background`/`--foreground`/`--primary`/`--popover`/`--border`/`--ring` 등을 LUM 기존 `--terminal-dark`/`--accent`/`--dim`에 매핑 (다크 only, `.dark` 클래스 안 씀), `@theme` 블록에 `--color-*` 노출 → utility class로 `bg-popover` `text-foreground` 등 사용 가능; deps 추가 — `@radix-ui/react-dialog`/`@radix-ui/react-slot`/`class-variance-authority`/`tw-animate-css` (Tailwind v4용 shadcn 표준 keyframes); `src/components/ui/button.tsx` (variant 6종: default/destructive/outline/secondary/ghost/link + size 4종) + `dialog.tsx` (Root/Trigger/Portal/Overlay/Content/Header/Footer/Title/Description/Close, 페이드+줌 애니메이션, sr-only 접근성 라벨) 수동 작성. **검증**: `tsc --noEmit` pass, `vite build` 성공 (1.2MB JS / 96KB CSS, 회귀 0), Vitest 131/131 pass. **다음**: 기존 손코딩 모달 점진적 마이그레이션 (단순한 것부터 — PasteGuardModal/WorkspacePanel deleteConfirm 등).
- [x] Phase 90: 메인 AI 백엔드 임베디드 라우팅 통합 (`current_engine` 죽은 참조 fix) — `ai.rs`의 `current_engine()` 호출이 Phase 88에서 `OnceCell` → `OnceLock<Mutex<Option<LoadedState>>>`로 API 변경 시 미업데이트, `embedded-ai` 빌드에서 컴파일 실패하는 dead code였음. `try_embedded_inference(prompt: &str, images: &[String]) -> Option<Result<String>>` cfg-gated 헬퍼로 통일 — `#[cfg(feature = "embedded-ai")]` 본체 + `#[cfg(not(...))]` stub 페어, `loaded_key().is_none()`이면 None, image 입력 있으면 None(GGUF 텍스트 모델 vision 미지원). 두 진입점에 동일 헬퍼 적용 — `call_xllm`(? explain · # generate · >> agent · git 커밋·diff 분석 등 비스트리밍 경로 일괄) + `stream_ai_command`(WarpInputBar 스트리밍 + AI Chat 사이드바, `xllm_token` 단일 이벤트로 풀 응답 emit). 임베디드 GGUF 우선 · 미로드 또는 image 시 HTTP 폴백 · 임베디드 실패 시 즉시 반환(폴백 안 함, 사일런트 마스킹 방지 + 명시적 에러 노출). 시그니처는 `xllm_body(... images: &[String])`과 통일(`bool` 변환 호출부 제거). 회귀 테스트 2개 — image-skip 가드 + non-feature build always-None 보장. **한계 (Phase A 예정)**: 임베디드 stream 경로는 단일 토큰 이벤트로 풀 응답 emit (실시간 토큰별 스트림은 mistralrs `stream_chat_request` API 도입 필요), `cancel_flag` 미전파 (추론 중 취소 불가). **영향**: 임베디드 7B 로드돼있으면 LUM의 모든 AI 흐름이 zero-network in-process 추론으로 자동 전환. Rust 74/0 (+2 신규), TS clean.
- [x] Phase 89: 모델 로드 진행 표시 + 잔여 /simplify 정리 — `mistralrs_inline.rs::load_model`에 `app: &AppHandle` 파라미터 추가 + `embed_load_progress` 이벤트 emit (시작/완료/오류 3 stage 메시지); `commands/embed.rs::embed_load_gguf`도 `tauri::AppHandle` 전파; `XllmPanel.tsx::EmbeddedInferenceDebug` UI — `loadElapsed: number` state + `setInterval(1s)` + `loadTimerRef: useRef`로 경과 초 카운터(7B GGUF 30~60초 / 30B+ 분 단위), `listen<string>("embed_load_progress")`로 stage 메시지 표시(버튼 아래 `font-mono text-purple-300/60`), 버튼 레이블 `"🔄 로드 중... ${loadElapsed}초"` 동적 업데이트(기존 `"로드 중... (수십초~분)"` 정적 텍스트에서 개선); finally에서 timer 정리 + state 리셋. **잔여 /simplify 정리(Phase 87/88 후속)**: `mistral_setup.rs` 자명한 `// GGUF 단일 파일 다운로드` / `// BF16 전체 repo` 주석 2개 제거 + `cancel_mistral_download` 락 패턴 `if let Ok(map) = cancel_map.lock()` → `cancel_map.lock().unwrap()`로 `download_mistral_model`과 통일; `mistralrs_inline.rs::infer_once` Arc 클론 후 `MutexGuard` 즉시 해제(블록 스코프) — 30s+ 추론 중 `loaded_key()` UI 폴링 차단 방지; `XllmPanel.tsx::EmbeddedInferenceDebug` `loadedKey.split("/").pop()` → `shortPath(loadedKey)` (`utils.ts` 기존 헬퍼 재사용, Windows `\` 경로 구분자도 처리) + `onUnload`에서 `setLoadedKey(null)` 직접 변경 → `refreshLoadedKey()` 호출(백엔드 일치성); `XllmPanel.tsx` VRAM cap 계산 3중 중복(`Math.round((config.vram_cap_override ?? MODE_DEFAULTS[...]) * 100)` 표시·슬라이더·복원 가드 3곳)을 `vramCapPct` 파생 const로 추출 + 불필요 `!== null` 가드 제거(타입이 `number | undefined`라 `null` 불가). 회귀 0 (Rust 74/0, TS clean).
- [x] Phase 88: 임베디드 AI 모델 핫스왑 (앱 재시작 없이 모델 교체) — `mistralrs_inline.rs` `OnceCell<Arc<Model>>` → `OnceLock<Mutex<Option<LoadedState>>>` 교체 (LoadedState = model + key 문자열); `load_model()` — 동일 키면 스킵, 다른 모델이면 기존 Drop(VRAM 해제) 후 새 모델 로드; `unload_model()` — 명시적 VRAM 해제; `loaded_key()` — try_lock 기반 동기 조회(로딩 중 None 반환); `embed.rs` 신규 커맨드 2개(`embed_unload`·`embed_loaded_info`) + `embed_load_gguf` → `load_model` 연결; `EmbeddedInferenceDebug` UI 개편 — `loadedKey` state로 현재 모델 파일명 헤더 표시, 로드 버튼 "로드됨/교체/로드" 3단계 레이블, [🗑 언로드] 버튼(로드 시만 표시), 동일 모델 선택 시 "✅ 로드됨 (재로드)" 표시. 72/0 테스트 통과.
- [x] Phase 87: 다운로드 취소 지원 — `DownloadCancelMap` state 재사용(models.rs와 공유), `hf_download_file`에 `cancel: &Arc<AtomicBool>` 파라미터 추가 + 청크 루프에서 폴링(취소 시 부분 파일 삭제), `download_mistral_model`이 State 주입으로 플래그 등록·완료 후 해제, `cancel_mistral_download` Tauri 커맨드 신규; ModelManager.tsx: `mistralBusy` boolean → `activeDownloadRepo: string|null`로 확장, 입력 폼 옆 [다운로드↔취소] 토글 버튼, 프리셋 카드 다운로드 중 펄스 애니 + 클릭→취소 전환. TS/Rust 회귀 0.
- [x] Phase 86: reqwest 기반 순수 Rust HF 다운로더 (외부 hf CLI 의존 제거) — Phase 85c에서 `~/tabbyAPI/.venv/Scripts/hf.exe` 삭제로 모델 다운로드 기능이 깨진 것을 수정. `find_hf_cli()`·`run_hf_download_streaming()`·`no_window_tokio()` 제거(Python subprocess 기반) → `hf_download_file()` (reqwest 스트리밍, 5% 단위 진행률 mistral_rs_log emit, 401/403 인증 오류 명확 안내) + `hf_list_repo_files()` (HF API `/api/models/{repo}` → `siblings[].rfilename` 파일 목록 조회, BF16 전체 다운로드용) 신규 추가. GGUF 단일 파일·BF16 전체 repo 다운로드 모두 새 구현으로 처리. 새 의존성 0 (reqwest·futures-util·serde_json 기존 dep 재사용). Rust 72/0 테스트 통과.
- [x] Phase 85e: XllmPanel.tsx dead field 정리 — `cache_mode`/`draft_model`/`speculative_n_draft` (TabbyAPI SSD 흔적) + "TabbyAPI autosplit_reserve" 라벨 제거. `handleSave`에서 해당 필드 null 고정 전달.
- [x] Phase 85d: ModelManager UI dead branch 통째 정리 (987줄 → 352줄, −64%) — Phase 85a-c가 백엔드/일부 UI까지만 정리하고 ModelManager 본체는 손 안 댔던 것 마무리. **제거**: ⚡ EXL2 직접 입력(TabbyAPI Fast Track) 섹션 + EXL2 카탈로그 list rendering(`download_model` → ~/tabby/models 흐름) + `isAppleSilicon` 상수 + 모든 Apple/MLX 분기 8군데 + `catalog.mlx` 참조 + mlx-community 외부 링크 + MLX-LM 시작 진행률 바 + `mlx_download_progress` 리스너 + `useModel`/`switch_xllm_model`/`get_xllm_model_info` 호출(TabbyAPI 시절 모델 스위치) + `loadProgress`/`loadingModel`/`loadedModelId`/`fetchLoaded` state + `customRepo`/`customRevision`/`handleCustomDownload`(EXL2 입력 폼) + `downloading`/`starting`/`downloadError`/`progressPct`/`_filesCompleted`/`_seenFiles`(EXL2 다운로드 진행 상태) + `categoryFilter` UI + `recommendedModel`/`gpuVramGb`/`totalMemoryGb` props(App.tsx 호출부 동시 정리) + `assignRole("heavy")`(85b 이후 `mistral_rs_*` config 필드 silent no-op). **데이터 소스 스왑**: 설치된 모델 탭이 `list_local_models`(~/tabby/models — 85c에서 삭제됨, 영원히 빈 화면) → **`list_mistral_models`(~/.lum_mistral_models, 실제 데이터)**로 전환 — XllmPanel 임베디드 모델 드롭다운과 동일 데이터 소스로 일관성 확보. **신규 백엔드**: `delete_mistral_model(safe_name)` Tauri 커맨드 — `~/.lum_mistral_models/<safe>/` 폴더 삭제, path traversal 방지(슬래시·`..` 거부); 그 전엔 사용자가 filesystem으로 직접 삭제해야 했음(85c처럼). **검증**: TS `tsc --noEmit` pass, `cargo check --no-default-features` pass, Rust 72 + TS 131 테스트 모두 통과(회귀 0). **남은 minor**: XllmPanel.tsx `cache_mode`/`draft_model`/`speculative_n_draft` 표시 라인(Phase 84 SSD 흔적, TabbyAPI 죽음으로 무의미) + `TabbyAPI autosplit_reserve` 라벨 — 다음 phase에서.
- [x] Phase 85c: 죽은 자산 정리 — 사용자 환경에서 ~33GB 회수 (`~/.lum_mistral_models/unsloth--Qwen3-Coder-30B-A3B-Instruct-GGUF/` 8.4GB candle MoE panic으로 사용 불가, `~/tabby/` 전체 19GB EXL2 모델 4개 + 관련 폴더, `~/tabbyAPI/` 6.4GB venv). C 드라이브 745GB→712GB (79%→75%). 임베디드 검증 모델 `bartowski--Qwen2.5-Coder-7B-Instruct-GGUF` 4.4GB만 유지. **부수 효과** — `download_mistral_model`의 `find_hf_cli`가 우선 찾던 `~/tabbyAPI/.venv/Scripts/hf.exe` 사라짐 → 시스템 PATH에 hf 없으면 모델 추가 다운로드 기능 깨짐. 다음 후보: hf-hub Rust crate 통합으로 외부 hf CLI 의존 제거 (Tauri 안에서 직접 다운로드).
- [x] Phase 85b: mistralrs LUM 프로세스 임베딩 (subprocess + HTTP 우회) + Heavy Track UI/backend 통째 제거 — `mistralrs = "0.8.1"` optional dep + CUDA features, `commands/mistralrs_inline.rs` GgufModelBuilder + send_chat_request 직접 호출, `commands/embed.rs` Tauri facade(cfg 분기 stub), XllmPanel 임베디드 디버그 UI + 모델 폴더/파일 드롭다운(`~/.lum_mistral_models/` 자동 스캔), `call_xllm()` 임베디드 분기 — 일반 `?`/`#` 입력이 모두 임베디드 7B로 응답 검증; mistralrs-server.exe spawn 코드(`install_mistral_rs`/`start_mistral_rs`/`stop_mistral_rs`/`check_mistral_rs_status`/`run_cargo_install_streaming`/`build_mistral_args`/`MistralLayeringOpts`/`validate_isq`/`MISTRAL_PROCESS`/`kill_on_port`)와 Heavy/Fast 라우팅(`router.rs` 통째)과 AppConfig의 `mistral_rs_*` 7개 필드 모두 삭제; `scripts/cargo-check-cuda.bat`+`tauri-dev-cuda.bat` MSVC env wrapper(npm chain에서 cl.exe PATH 손실 우회), Kaspersky 제외 추가 후 빌드 정상 (14m 36s 첫 빌드, 캐시 hit 13s). **검증**: 7B Q4_K_M GGUF로 `?` 프리픽스 입력 → 한국어 자연어 응답 (subprocess 0, HTTP 0). Phase 83b candle MoE panic은 그대로(30B-A3B만 영향, dense 7B는 정상). **Lessons learned**: mistralrs high-level wrapper도 candle 0.10.2의 `indexed_moe_forward` 한계 동일 — MoE는 candle 업스트림 fix 대기, dense 모델은 임베딩으로 즉시 동작. **누적**: 9 commits / ~-3000줄 net (Phase 85a + 85b 합산).
- [x] Phase 85a: TabbyAPI 통째 제거 — *AI Agent 시연 부적합* (CrewAI tool_calls 미지원·시작 시 멈춤·좀비 프로세스·Exit hook deadlock 등) 검증 후 mistral.rs 단일 엔진으로 통일 결정. 3개 commit으로 단계별 안전 정리: ① `xllm_url() → mistral.rs URL` 의미 전환(commit ca14fbc, 호출부 unchanged로 변경 surface 최소화) → ② 백엔드 통째 제거(commit f8250b9, `tabbyapi_setup.rs` 957줄 파일 삭제 + `lib.rs` invoke_handler 7개 entry + `App.tsx` 자동 시작 useEffect, `kill_on_port`는 `mistral_setup.rs`로 inline 이동) → ③ 프론트엔드 dead UI 정리(commit 8ac7be7, `XllmPanel.tsx` TabbyAPI JSX 섹션 + 11개 useState + 4개 useCallback + 3개 listener, `ModelManager.tsx` `get_platform_arch` invoke 제거 + `isAppleSilicon = false` 상수화). **누계 ~1200줄 net 삭제** + Rust 98→91/0(tabbyapi 7개 테스트 제거됨, 회귀 0). 실동작 검증 완료 — mistral.rs로 추론 정상. **남은 작업**: 85a-4(잔여 deprecated UI: cache_mode/role/SSD/EXL2 카테고리/xllm_base_url 입력란) + 85b(mistralrs-core를 Rust crate으로 LUM 내부 직접 임베딩 — 좀비 프로세스 0 + 통신 오버헤드 0).
