# 초기 베타 준비 상태

최종 갱신: 2026-08-07

## 확인 완료

| 영역 | 확인 근거 | 상태 |
| --- | --- | --- |
| 프론트엔드 빌드 | `npm run build` 성공 | 완료 |
| 프론트엔드 단위 테스트 | 이전 전체 실행 159 파일, 2,150 테스트 통과. 최근 `TerminalPane` 163건, `InspectorSummaryOverviewCard` 6건 통과 | 완료 |
| Rust 단위 테스트 | `cargo test` 431건 통과 | 완료 |
| 브라우저 스모크 | 온보딩, 탭, 인스펙터 분석, 추천 명령, 헤더 오버레이 등의 대상 E2E 통과 | 완료 |
| 입력 도크 | AI 대화가 열린 상태에서도 입력 도크가 축소되지 않도록 `flex: 0 0 auto` 적용 | 완료 |
| 인스펙터 개요 | 최대 344px 폭의 인스펙터에서 2열 카드를 1열로 변경 | 완료 |
| 임베디드 AI | `npm run tauri:dev:metal`로 기동 후 `@local` 요청이 `embedded local ready` 응답 | 완료 |
| xLLM 서버 | MLX `Qwen2.5-Coder-7B-Instruct-4bit`가 `127.0.0.1:8080`의 모델 목록 및 SSE 응답 제공 | 완료 |
| Whisper 실행 환경 | Whisper CLI, Python 모듈, 합성 음성의 base 모델 전사 실행 확인 | 완료 |

## 남은 수동 확인

| 항목 | 절차 | 완료 기준 |
| --- | --- | --- |
| 앱 내 xLLM 라우팅 | 입력 바에 `@xllm Reply only XLLM_READY77` 입력 | `XLLM_READY77` 응답과 xLLM 백엔드 표시 확인 |
| 실제 마이크 입력 | 마이크 버튼을 눌러 macOS 권한 승인 후 한국어 문장 입력 | 전사 텍스트가 입력 바에 삽입되고 AI/셸 실행 가능 |
| 전체 Vitest 재실행 | 리소스가 한가한 상태에서 `npm test -- --run` 실행 | 159 파일 전체 요약이 성공으로 종료 |

## 알려진 품질·운영 메모

- Vite 빌드는 메인 번들이 500kB를 초과한다는 경고를 낸다. 현재 기능 오류는 아니며, 베타 이후 화면 단위 코드 분할 대상으로 관리한다.
- Whisper `tiny` 모델은 짧은 영어 문장도 부정확했다. `base` 모델은 전사 파이프라인을 정상 실행했지만, 한국어 품질은 실제 마이크로 확인해야 한다.
- 테스트 목적으로 기동한 Metal 앱은 검증 후 종료했다. xLLM 서버는 `127.0.0.1:8080`에서 별도로 실행 중일 수 있다.
